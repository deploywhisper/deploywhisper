{
  "name": "deploywhisper-ui-a11y",
  "private": true,
  "version": "0.0.0",
  "description": "Dev-only browser and screen-reader verification for DeployWhisper UI flows.",
  "scripts": {
    "test:ui-review": "npm --prefix frontend exec -- playwright test --config=frontend/playwright.config.ts",
    "ui:dev": "npm --prefix frontend run dev",
    "ui:build": "npm --prefix frontend run build",
    "ui:test": "npm --prefix frontend run test",
    "ui:typecheck": "npm --prefix frontend run typecheck",
    "ui:gen-api": "npm --prefix frontend run gen-api"
  }
}
